import { TaskHoverDirective } from './task-hover.directive';

describe('TaskHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskHoverDirective();
    expect(directive).toBeTruthy();
  });
});
